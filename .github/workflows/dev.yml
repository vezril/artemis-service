name: Dev publish

# Every push to development publishes an explicitly-unstable image (:dev and :dev-<short-sha>)
# after the tests pass, so the homelab can trial unreleased builds deliberately. Runs only on
# pushes to this repo's development branch — fork PRs cannot push here, so secrets stay safe.
on:
  push:
    branches: [development]

env:
  IMAGE_NAME: artemis
  # read:packages PAT to resolve the Lexicon jars from GitHub Packages (see ci.yml).
  LEXICON_TOKEN: ${{ secrets.LEXICON_TOKEN }}

jobs:
  dev-publish:
    name: Test and publish dev image
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Ensure Docker Hub credentials are present
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
        run: |
          if [ -z "$DOCKERHUB_USERNAME" ] || [ -z "$DOCKERHUB_TOKEN" ]; then
            echo "ERROR: DOCKERHUB_USERNAME / DOCKERHUB_TOKEN secret is missing"
            exit 1
          fi

      - uses: ./.github/actions/setup-scala

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Run tests
        run: sbt -batch test

      - name: Build image
        run: sbt -batch server/Docker/publishLocal

      - name: Tag and push dev + dev-<short-sha>
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
        run: |
          VERSION=$(sbt -Dsbt.log.noformat=true -batch -error 'print server/version' | tail -n1 | perl -pe 's/\e\[[0-9;]*[a-zA-Z]//g' | tr -d '[:space:]')
          SHORT_SHA="$(echo "${GITHUB_SHA}" | cut -c1-7)"
          LOCAL="$IMAGE_NAME:$VERSION"
          for TARGET in "$DOCKERHUB_USERNAME/$IMAGE_NAME:dev" "$DOCKERHUB_USERNAME/$IMAGE_NAME:dev-$SHORT_SHA"; do
            docker tag "$LOCAL" "$TARGET"
            docker push "$TARGET"
          done
